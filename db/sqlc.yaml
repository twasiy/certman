version: "2"
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "sqlite"
    gen:
      go:
        package: "base"
        out: "base"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
